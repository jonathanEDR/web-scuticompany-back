# ============================================================================
# CONFIGURACIÓN SISTEMA DE AGENTES AI - WEB SCUTI
# ============================================================================

# ============================================================================
# OPENAI CONFIGURATION (Opcional - sin esto funciona modo limitado)
# ============================================================================
# Obtén tu API key en: https://platform.openai.com/api-keys
OPENAI_API_KEY=tu_openai_api_key_aqui

# ============================================================================
# CONFIGURACIONES EXISTENTES (mantener las que ya tienes)
# ============================================================================

# Database
MONGODB_URI=mongodb://localhost:27017/web-scuti
# O para MongoDB Atlas:
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/webscuti

# Clerk Authentication
CLERK_SECRET_KEY=tu_clerk_secret_key
CLERK_PUBLISHABLE_KEY=tu_clerk_publishable_key

# Server
PORT=5000
NODE_ENV=development

# Cloudinary
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# Email (Resend)
RESEND_API_KEY=tu_resend_key

# Frontend URLs (para CORS)
FRONTEND_URL=http://localhost:5173,http://localhost:5174

# ============================================================================
# CONFIGURACIONES DEL SISTEMA DE AGENTES (Opcionales)
# ============================================================================

# Configuración de agentes (valores por defecto si no se especifican)
AGENT_MAX_CONCURRENT_TASKS=5
AGENT_TASK_TIMEOUT=30000
AGENT_RETRY_ATTEMPTS=3
AGENT_HEALTH_CHECK_INTERVAL=60000

# Límites de contenido
AGENT_MAX_TAGS_PER_POST=10
AGENT_MIN_CONTENT_LENGTH=300
AGENT_SEO_SCORE_THRESHOLD=70

# Cache de OpenAI
OPENAI_CACHE_TIMEOUT=300000  # 5 minutos en ms
OPENAI_MAX_CACHE_SIZE=100

# Rate Limiting para agentes
AGENT_RATE_LIMIT_WINDOW=900000  # 15 minutos
AGENT_RATE_LIMIT_MAX_REQUESTS=30
AGENT_AI_RATE_LIMIT_WINDOW=300000  # 5 minutos
AGENT_AI_RATE_LIMIT_MAX_REQUESTS=10

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================

# 1. OPENAI_API_KEY es OPCIONAL
#    - Sin ella: el sistema funciona con análisis básico y respuestas de respaldo
#    - Con ella: capacidades completas de IA (GPT-4) para comandos naturales
#
# 2. El sistema está diseñado para funcionar sin dependencias externas
#    - Utiliza tus modelos y utilidades existentes
#    - OpenAI solo mejora la experiencia, no es obligatorio
#
# 3. Rate Limiting
#    - En desarrollo está deshabilitado para facilitar testing
#    - En producción se aplicarán los límites configurados
#
# 4. Roles y Permisos
#    - Usa tu sistema existente de Clerk + roles
#    - Diferentes endpoints requieren diferentes permisos:
#      * GET endpoints: más permisivos
#      * POST/comandos: requieren content_manager o admin
#      * Reinicialización: solo admin

# ============================================================================
# EJEMPLOS DE USO RÁPIDO
# ============================================================================

# Una vez configurado, puedes probar:

# 1. Health Check (sin autenticación)
# GET http://localhost:5000/api/agents/health

# 2. Capacidades disponibles (sin autenticación) 
# GET http://localhost:5000/api/agents/capabilities

# 3. Comando de lenguaje natural (requiere auth + roles)
# POST http://localhost:5000/api/agents/command
# Body: { "command": "analizar rendimiento del blog" }

# 4. Optimización directa (requiere auth + content_manager)
# POST http://localhost:5000/api/agents/blog/optimize
# Body: { "slug": "mi-primer-post" }

# ============================================================================